# Sequence 분석

이야기의 질문·주장·감정 변화를 기준으로 sequence를 순서대로 제안한다.

- ID, source unit ID 목록, scene/group ID 또는 상위 범위는 만들지 않는다.
- 각 sequence 안에 scene_groups와 scenes를 중첩해서 출력한다.
- 배열 순서가 곧 이야기 순서다.
- 구조 ID와 범위는 런타임 컴파일러가 생성한다.

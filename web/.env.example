# 복사해서 .env 로 사용: cp .env.example .env

WEB_PORT=8080

# [방식 A · 기본] nginx 가 /api 를 서버로 프록시 (CORS 불필요, 런타임 변경 가능)
# 로컬에서 서버 레포를 3000 포트로 띄웠다면 기본값 그대로 사용
API_UPSTREAM=http://host.docker.internal:3000

# [방식 B] 브라우저가 API 를 직접 호출. 빌드 시 고정되며 서버에 CORS_ORIGIN 설정 필요
VITE_API_BASE_URL=
